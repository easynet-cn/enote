## 3. 软件功能概述

### 3.1 功能结构图

```
ENote 智能笔记管理系统
├── 笔记本管理
│   ├── 新建笔记本
│   ├── 编辑笔记本
│   ├── 删除笔记本
│   ├── 拖拽排序笔记本
│   └── 按笔记本筛选笔记
├── 标签管理
│   ├── 新建标签
│   ├── 编辑标签
│   ├── 删除标签
│   ├── 拖拽排序标签
│   └── 按标签筛选笔记
├── 笔记管理
│   ├── 新建笔记
│   ├── 编辑笔记
│   ├── 删除笔记（移入回收站）
│   ├── 回收站（恢复/永久删除/清空）
│   ├── 搜索笔记（FTS5 全文搜索）
│   ├── 笔记置顶
│   ├── 笔记加密/解密
│   ├── 关联笔记（双向链接）
│   ├── 在新窗口中打开
│   └── 笔记设置（笔记本/标签分配）
├── 笔记模板
│   ├── 模板管理（新建/编辑/删除，支持富文本和 Markdown 双模式）
│   ├── 使用模板创建笔记（自动继承内容类型）
│   └── 保存笔记为模板（保留内容类型）
├── 富文本编辑器
│   ├── 文本格式化（粗体、斜体、下划线、删除线等）
│   ├── 标题层级（H1-H6）
│   ├── 字体与字号设置
│   ├── 文本对齐
│   ├── 列表（无序、有序、任务列表）
│   ├── 缩进控制
│   ├── 引用与代码块
│   ├── 链接与图片插入
│   ├── 表格编辑
│   ├── 文字与背景颜色
│   ├── 查找与替换
│   ├── 目录导航
│   ├── 撤销与重做
│   ├── 内容块拖拽排序
│   ├── 数学公式渲染
│   ├── 图片懒加载
│   ├── 智能粘贴（清理外部格式）
│   └── 图片本地文件存储
├── Markdown 编辑器
│   ├── 源码编辑
│   ├── 实时预览
│   ├── 分屏模式（水平/垂直）
│   └── 数学公式渲染
├── 历史记录
│   ├── 版本列表
│   └── 内容对比查看
├── 导出功能
│   ├── Word 文档
│   ├── Markdown 文件
│   ├── 印象笔记格式
│   ├── JSON 格式
│   └── XML 格式
├── 导入功能
│   ├── 印象笔记导入
│   ├── 有道笔记导入
│   └── Notion 导入
├── 数据备份与恢复
│   ├── 导出备份（SQL / Excel / CSV）
│   ├── 导入恢复
│   └── 自动定时备份
├── 应用设置
│   ├── 主题切换（浅色/深色/跟随系统）
│   ├── 多语言切换（中文/英文）
│   ├── 自动备份配置
│   ├── 安全设置（锁屏密码/超时锁定/最小化锁定）
│   └── MCP 设置（总开关/各工具独立开关）
├── MCP（AI 工具集成）
│   ├── 笔记 CRUD（搜索/获取/创建/更新/删除）
│   ├── 笔记本管理（列出/创建）
│   ├── 标签管理（列出/创建）
│   ├── 笔记统计
│   ├── 权限控制（动态工具列表/调用校验）
│   └── 操作来源追溯（历史记录标注）
├── 命令面板（Ctrl+P 快速操作）
├── 系统托盘
│   ├── 最小化到托盘
│   ├── 托盘菜单（显示窗口/退出）
│   └── 单击切换窗口显示
└── 应用启动与初始化
    ├── 命令行指定配置文件（--config）
    ├── 自动创建配置与数据库
    ├── 启动锁屏验证
    ├── 数据库自动迁移
    └── 启动异常提示
```

### 3.2 界面布局

ENote 采用响应式三栏布局设计，根据屏幕宽度自动适配不同设备：

#### 桌面模式（宽度 > 1024px）

三栏并排显示，从左至右依次为：

**侧边栏（左侧）：** 宽度 256 像素，可折叠为 48 像素的图标栏。包含新建笔记按钮、笔记本列表、标签列表，底部工具栏包含导入笔记、数据备份、模板管理、设置和回收站等图标按钮（鼠标悬浮显示功能提示）。

**笔记列表区（中间）：** 默认宽度 320 像素，支持拖拽调整宽度（200-600 像素范围），可完全折叠。包含当前笔记本名称标题、搜索栏、笔记卡片列表和分页控件。置顶的笔记在列表顶部显示，带有置顶标识。

**编辑器区域（右侧）：** 占据剩余全部宽度。顶部为功能丰富的编辑工具栏，下方为笔记标题输入框和正文编辑区，编辑区下方为关联笔记面板（可展开/折叠），底部为状态栏显示光标位置与字数统计。

#### 平板模式（宽度 640px ~ 1024px）

双栏显示：笔记列表（固定 320px）+ 编辑器。侧边栏以覆盖层形式弹出，点击笔记列表左上角的菜单图标打开，点击遮罩层或关闭按钮收起。拖拽调整宽度和折叠按钮在此模式下隐藏。

#### 手机模式（宽度 < 640px）

单视图全屏切换：笔记列表和编辑器分别全屏显示，通过滑动动画切换。在笔记列表中选择笔记后自动切换到编辑器视图，编辑器左上角显示返回按钮回到列表。侧边栏同样以覆盖层形式弹出。

> **提示：** 布局模式可在设置中手动切换（自动/桌面/平板/手机），也可通过快捷键 Ctrl+Shift+M 循环切换。详见 [14.3 布局模式设置](14-settings.md#143-布局模式设置)。
